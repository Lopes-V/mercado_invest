"use client";
import { useState } from "react";
export function SettingsForm() { const [hour, setHour] = useState("22"); const [message, setMessage] = useState(""); async function save(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const response = await fetch("/api/settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ telegramSummaryHourBrt: Number(hour) }) }); setMessage(response.ok ? "Horário salvo." : "Não foi possível salvar."); } return <form onSubmit={save} className="mt-6 max-w-sm space-y-3"><label className="block">Hora BRT<select value={hour} onChange={(event) => setHour(event.target.value)} className="ml-3 rounded bg-white/10 p-2">{Array.from({ length: 24 }, (_, value) => <option key={value} value={value}>{String(value).padStart(2, "0")}:00</option>)}</select></label><button className="rounded bg-emerald-400 px-3 py-2 font-medium text-black">Salvar</button>{message ? <p role="status">{message}</p> : null}</form>; }
