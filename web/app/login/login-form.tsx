"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function LoginForm() { const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [pending, setPending] = useState(false); const router = useRouter(); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setError(""); try { const response = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ password }) }); if (!response.ok) { setError("Não foi possível autenticar."); return; } router.replace("/dashboard"); router.refresh(); } catch { setError("Autenticação indisponível."); } finally { setPending(false); } } return <form onSubmit={submit} className="w-full max-w-sm space-y-5 rounded-xl border border-white/10 bg-white/5 p-6 shadow-2xl"><div><p className="text-xs font-semibold tracking-[0.2em] text-emerald-300">MERCADO INVEST</p><h1 className="mt-2 text-2xl font-semibold">Acesso administrativo</h1></div><label className="block text-sm" htmlFor="password">Senha<input id="password" name="password" type="password" autoComplete="current-password" required value={password} onChange={(event) => setPassword(event.target.value)} className="mt-2 w-full rounded-md border border-white/15 bg-black/30 px-3 py-2" /></label>{error ? <p role="alert" className="text-sm text-red-300">{error}</p> : null}<button disabled={pending} className="w-full rounded-md bg-emerald-400 px-3 py-2 font-medium text-black disabled:opacity-60">{pending ? "Entrando…" : "Entrar"}</button></form>; }
