"use client";
import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { ChartPoint } from "@/lib/data/market";
export function MetricChart({ title, points }: { title: string; points: ChartPoint[] }) { if (!points.length) return <section className="rounded-xl border border-white/10 p-4"><h2 className="font-medium">{title}</h2><p className="mt-3 text-sm text-slate-400">Dados históricos indisponíveis.</p></section>; const data = points.map((point) => ({ ...point, numeric: Number(point.value) })); return <section className="rounded-xl border border-white/10 p-4"><h2 className="font-medium">{title}</h2><div className="mt-3 h-56"><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><XAxis dataKey="at" hide /><YAxis width={70} /><Tooltip labelFormatter={(value) => new Date(String(value)).toLocaleString("pt-BR")} /><Line type="monotone" dataKey="numeric" stroke="#86efac" dot={false} /></LineChart></ResponsiveContainer></div></section>; }
